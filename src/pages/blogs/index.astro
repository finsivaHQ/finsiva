---
import Layout from "../../layouts/Layout.astro";
import EditorialStandardsCard from "../../components/EditorialStandardsCard.astro";
import { getSortedPosts } from "../../utils/blog";

const posts = getSortedPosts();
---

<Layout title="Tax Guides & Blog - Global Tax Information & Calculations | Finsiva" description="Explore tax guides, calculations, and updates for India, Hong Kong, Pakistan, US, UK, and global jurisdictions on Finsiva.">
  <div class="min-h-screen bg-canvas dark:bg-canvas">
    <main class="min-h-screen">
      <div class="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8">
        <h1 class="text-4xl font-bold text-ink mb-4 dark:text-on-primary">Tax Guides & Blog</h1>
        <p class="text-lg text-body dark:text-muted mb-12">Country-specific tax guides, statutory deduction rules, filing procedures, and cross-border comparisons.</p>
        
        <div class="grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-3">
          {posts.map(post => {
            const countryTag = post.slug.startsWith('hong-kong') ? 'Hong Kong Tax' : post.slug.startsWith('pakistan') ? 'Pakistan Tax' : post.slug.includes('india') || post.slug.includes('80c') || post.slug.includes('hra') || post.slug.includes('tds') || post.slug.includes('regime') ? 'India Tax' : 'International Tax';
            return (
              <article class="rounded-xl border border-hairline bg-canvas p-6 transition hover:border-primary hover:shadow-lg dark:border-overlay-lighter dark:bg-surface dark:hover:border-primary">
                <span class="inline-block rounded-md bg-primary/10 px-2.5 py-1 text-xs font-semibold text-primary uppercase tracking-wider dark:bg-surface/20 dark:text-link mb-3">{countryTag}</span>
                <h2 class="text-xl font-semibold text-ink mb-2 dark:text-on-primary">
                  <a href={`/blogs/${post.slug}/`} class="hover:text-primary dark:hover:text-link">{post.title}</a>
                </h2>
                <p class="text-sm text-muted mb-4 dark:text-muted">{post.description}</p>
                <div class="flex items-center gap-4 text-sm text-muted dark:text-muted">
                  <span>By {post.author}</span>
                  <span aria-hidden="true">·</span>
                  <span>{post.date}</span>
                  <span>{post.readTime}</span>
                </div>
              </article>
            );
          })}
        </div>

        <EditorialStandardsCard class="mt-12" />
      </div>
    </main>
  </div>
</Layout>
